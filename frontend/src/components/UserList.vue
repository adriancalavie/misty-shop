<template></template>

<script setup>
import { ref, computed } from "vue";

const data = ref(null);
const loading = ref(true);
const error = ref(null);

function fetchData() {
  // TODO Implement this
}
</script>
